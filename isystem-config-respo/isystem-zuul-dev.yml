spring:
  application:
    name: isystem-zuul

server:
  port: 8769

zuul:
  routes:
    backend-admin:
      path: /backend/admin/**
      serviceId: isystem-web-admin
    backend-posts:
      path: /backend/posts/**
      serviceId: isystem-web-posts